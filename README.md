> 생성: 2026-08-29 | 작성자: 김민섭+Claude | 맥락: 커버 영상 하단 악보 오버레이를 잘라 전체 악보로 이어붙이는 도구. 밴드 멤버 공유용

# Score Clipper

커버 영상(드럼·기타·베이스 등) 하단에 4마디씩 표시되는 악보를 자동으로 잘라내
전체 악보 한 장(PNG/PDF)으로 이어붙인다.

## 동작 원리

1. 영상을 일정 간격(기본 0.5초)으로 샘플링해 악보 띠 영역만 크롭
2. 연속 프레임 간 이미지 차이로 페이지 넘김 순간 감지
3. 페이지별 안정 구간의 중간 프레임을 대표로 선택
4. 반복 구절 보존: 중복 비교는 직전 페이지와만 한다
   (전체 비교하면 마디 번호만 다른 반복 구절이 삭제되는 버그 - 실제로 겪음)
5. 세로로 이어붙여 출력

제약: 페이지 넘김 방식 전용. 연속 스크롤 악보는 미지원.

## 웹앱 (index.html)

- 배포: https://score-clipper.vercel.app (Vercel, GitHub main 푸시 시 자동 배포)
- 서버 없는 정적 페이지. 영상은 브라우저 밖으로 전송되지 않음
- 사용법: 영상 파일 업로드 -> 악보 영역 드래그 지정 -> 추출 -> PNG 다운로드 / PDF 저장 / 인쇄
- 유튜브 URL 직접 입력은 미지원 (브라우저 보안 제약). yt-dlp 등으로 파일 저장 후 업로드
- 로컬 실행: `python3 -m http.server 8765` 후 http://localhost:8765
- 테스트 훅: `?src=파일URL` 쿼리로 자동 로드 (같은 출처 파일)

## 유튜브 링크 모드 (server.py, v3)

브라우저는 유튜브를 직접 못 받으므로(차단), 로컬 서버가 yt-dlp로 대신 받아 웹앱에 넘긴다.
서버가 감지되면 웹앱 1단계에 링크 입력칸이 자동으로 나타난다 (없으면 파일 업로드만).

```
./start.sh
```

- 서버(포트 8765) + Cloudflare Tunnel을 함께 실행. 출력되는
  `https://xxxx.trycloudflare.com` 주소를 밴드 멤버에게 공유 (재실행 시 주소 바뀜)
- 필요: `brew install yt-dlp cloudflared` (ffmpeg 포함)
- 안전장치: 유튜브 링크만 허용, 20분 초과 영상 거부, 캐시 24시간/5GB 제한
- 맥이 꺼져 있으면 멤버들은 Vercel 주소에서 파일 업로드로 이용 (자동 폴백)

## CLI (cli/score_stitch.py)

유튜브 URL부터 한 번에 처리할 때 사용 (yt-dlp + ffmpeg 필요).

```
yt-dlp --no-playlist -f "bv*[height<=1080][ext=mp4]" -o video.mp4 "<URL>"
pip install -r cli/requirements.txt
python cli/score_stitch.py video.mp4 --y 940 --height 140 --fps 2 --out score_out
```

`--y`, `--height`는 악보 띠의 시작 y좌표와 높이(px). 프레임 하나 뽑아서 확인 후 지정.

## 마디 하이라이트 대응 (v2)

현재 마디를 따라다니는 하이라이트·커서가 있는 영상 지원:

- 페이지 넘김 감지를 가로 8구간 다수결로 판정 (하이라이트 이동은 1-2구간만 바뀜)
- 페이지마다 최대 7프레임을 픽셀 중앙값으로 합성해 움직이는 표시를 제거

한계: 표시가 한 위치에 페이지 시간의 절반 이상 머물면(중앙값 표본 과반) 지워지지 않음.

## 검증 이력

- 2026-08-29: 「心絵」 드럼 커버(4:16, 1080p)로 검증. 44페이지(176마디) 누락 없이 추출 확인
- 2026-08-29: 같은 영상에 움직이는 노란 마디 박스를 합성한 테스트 영상으로 v2 검증.
  플레인과 동일한 페이지 세트 추출 + 결과물 노란 픽셀 0.000% (웹앱·CLI 모두)

## 저작권 주의

개인 연습용. 추출한 악보의 공개 배포는 원 영상 제작자의 저작권 침해 소지가 있음.
